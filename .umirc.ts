import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'BioMiner UI',
  favicon: 'https://rapex.prophetdb.org/gene.png',
  logo: 'https://rapex.prophetdb.org/gene.png',
  outputPath: 'docs-dist',
  mode: 'site',
  // more config: https://d.umijs.org/config
});
